export class Row {
    label: string;
    icon: string;
    color: string;

    constructor(label: string, icon: string, color: string) {
       this.label = label;
       this.icon = icon;
       this.color = color;
    }
 }